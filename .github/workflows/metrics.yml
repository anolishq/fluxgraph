name: Metrics

on:
  push:
    branches: [main]
  schedule:
    - cron: '0 6 * * 1'  # Weekly Monday 6am UTC
  workflow_dispatch:

jobs:
  collect:
    uses: anolishq/.github/.github/workflows/metrics.yml@0f4d8cd3c04dcb17fe128248a147b237c1465e0c # v2
