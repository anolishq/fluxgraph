name: Metrics

on:
  push:
    branches: [main]
  schedule:
    - cron: '0 6 * * 1'  # Weekly Monday 6am UTC
  workflow_dispatch:

jobs:
  collect:
    uses: anolishq/.github/.github/workflows/metrics.yml@v1
